{
  "name": "napajs",
  "version": "0.4.13",
  "author": "napajs",
  "main": "./lib/index.js",
  "types": "./types/index.d.ts",
  "devDependencies": {
    "mocha": ">= 3.4.2",
    "typescript": ">= 2.2.1",
    "@types/node": ">= 7.0.8",
    "@types/mocha": ">= 2.2.0"
  },
  "scripts": {
    "install": "cmake-js compile",
    "prepublish": "tsc -p lib",
    "pretest": "tsc -p test",
    "test": "mocha test --recursive"
  }
}
